// setup config vars
BlackBox = BlackBox || {
  // global config for client + server
  global: {
    log: {
      function: console.info,
      context: console
    }
  },
  client: {},
  server: {}
};
